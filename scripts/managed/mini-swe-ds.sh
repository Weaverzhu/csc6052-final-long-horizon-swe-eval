set -xg FRAMEWORK mini-swe-agent
set -xg MODEL deepseek-v4-flash
set -xg RUNS_ROOT "$PWD/.agent_runs"
set -xg TEMPLATE project-1
set -xg STAGE_NUM 1
set -xg END_STAGE 6
set -xg API_BASE https://api.deepseek.com
set -xg MODEL_BACKEND openai-compat
